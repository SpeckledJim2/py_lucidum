dataset:
  path: ../datasets/motor_premiums.parquet
  response_numerator: PREMIUM
  denominator: null
  sample_column: SAMPLE
  training_value: training
  test_value: test
  validation_value: validation

model:
  id: EXTERNAL_BUILD-config-glm
  label: EXTERNAL_BUILD config GLM
  formula_path: external_glm_formula.txt
  family: tweedie
  family_parameter: 1.2
  link: log
  fit_intercept: true
  training_scope: training
  regularization:
    alpha: 0.0
    l1_ratio: 0.0
    scale_predictors: false

output:
  model_results_root: ../local/external_model_results
  install_in_lucidum: true
  replace_existing: true
